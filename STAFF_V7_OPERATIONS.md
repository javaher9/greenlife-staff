# GreenLife Staff v7 — Operations + Automatic KPI

## قابلیت‌های جدید
- شیفت‌های اختصاصی هر شعبه و تخصیص شیفت به هر کارمند در هر روز
- محاسبه تأخیر بر اساس شیفت همان روز، نه ساعت ثابت شعبه
- نمایش اضافه‌کاری بر اساس پایان شیفت
- مأموریت و مرخصی تأییدشده در گزارش حضور به‌عنوان غیبت محسوب نمی‌شود
- درخواست اصلاح ورود/خروج توسط کارمند و تأیید/رد توسط مدیر با Audit trail
- KPI خودکار ۳۰ روزه: حضور ۴۰٪، Taskها ۲۵٪، گزارش روزانه ۲۰٪، تحقق درآمد شعبه ۱۵٪
- جریمه خودکار برای هر شب گزارش روزانه ارسال‌نشده
- اعلان داخل Staff برای کارمند شامل تعداد شب‌های گزارش ارسال‌نشده در ۳۱ روز اخیر
- API وضعیت پرسنل برای دستیار مدیریتی شامل ورود/خروج، گزارش‌های جاافتاده و KPI خودکار

## جریمه گزارش روزانه
مقدار پیش‌فرض هر شب: `-2` امتیاز. قابل تغییر با:
`REPORT_MISSING_PENALTY=-2`

مرخصی/ماموریت تأییدشده جریمه گزارش نمی‌گیرد. هر تاریخ فقط یک بار جریمه می‌شود.

### اجرای شبانه
روی سرور بعد از Deploy، این فرمان باید هر شب اجرا شود:
`python manage.py process_nightly_staff`

مثال cron برای ساعت ۰۰:۰۵ تهران:
`5 0 * * * cd /path/to/staff && docker compose exec -T web python manage.py process_nightly_staff`

اگر scheduler دیگری روی سرور وجود دارد، همان فرمان را با آن اجرا کنید.

## Deploy
1. Backup دیتابیس
2. `docker compose build`
3. `docker compose up -d`
4. `docker compose exec web python manage.py migrate`
5. تعریف شیفت‌ها از `/shifts/`
6. تنظیم Cron شبانه

## API مدیریتی جدید
`GET /api/management/employee/status/?name=هاشمی`
Header: `X-Staff-API-Key: ...`

خروجی شامل وضعیت امروز، تعداد شب‌های گزارش جاافتاده، تاریخ‌های شمسی و KPI خودکار ۳۰ روزه است.
