# GreenLife Staff v20 — Manager Dashboard + Attendance Control

## 1. داشبورد مدیر
- داشبورد زنده امروز با KPIهای حاضر، دیرکرد، ورود ثبت‌نشده، گزارش ثبت‌نشده، Task عقب‌افتاده و GPS تأییدنشده.
- بخش «نیازمند اقدام امروز» برای نمایش مستقیم افراد مسئله‌دار.
- جدول کامل پرسنل با شیفت مورد انتظار، ورود، خروج، وضعیت GPS، گزارش روزانه و Task عقب‌افتاده.
- بروزرسانی خودکار هر 60 ثانیه.
- مدیر شعبه فقط شعبه خودش را می‌بیند؛ Admin می‌تواند شعبه را انتخاب کند.

## 2. حضور و غیاب ضدتقلب
- ورود در شعبه‌هایی که Geofence فعال است فقط با GPS و داخل شعاع مجاز ثبت می‌شود.
- GPS با دقت بیشتر از 200 متر رد می‌شود.
- فاصله واقعی از شعبه ذخیره و در داشبورد مدیر نمایش داده می‌شود.
- خروج نیز در صورت فعال بودن Geofence فقط با GPS داخل محدوده ثبت می‌شود.
- تلاش ناموفق رکورد حضور جعلی ایجاد نمی‌کند.
- دیرکرد نسبت به Shift واقعی هر شخص محاسبه و در داشبورد مدیر نمایش داده می‌شود.
- درخواست اصلاح حضور موجود حفظ شده است.

نکته: Geofence باید برای هر Branch در Django Admin با Latitude/Longitude/Radius فعال و تنظیم شود.
Migration جدید لازم نیست؛ فیلدهای Geofence از قبل در پروژه وجود داشتند.
